# Environment for quality control of the mapping of the reads to the assembly
---
name: mapping
channels:
    - conda-forge
    - bioconda
    - defaults
dependencies:
    - bowtie2=2.3.4
    - samtools=1.9
